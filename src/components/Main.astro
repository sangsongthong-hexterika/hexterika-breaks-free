<main id="main">
    <section>
        <h1>Explore My Passions!</h1>
        <p>
            Discover the many sides of Hexterika-Breaks-Free, from tech and Cybersecurity to MMA, scuba diving, and beyond. Click on any topic to learn more about my journey and projects.
        </p>
    </section>
    <section>
        <p>Add cards</p>
    </section>
</main>

<!--CSS Stylings-->
<style>
    main {
        align-items: center;
    }
    
    section {
        margin-bottom: 2rem;
    }

    h1 {
        font-size: 3rem;
    }

    h2 {
        font-size: 2.5rem;
    }

    h3 {
        font-size: 2rem;
    }

    p {
        font-size: 1rem;
        font-weight: bold;
        padding-left: 2rem;
        padding-right: 2rem;
    }

    a {
        padding-right: 2rem;
        padding-left: 2rem;
    }

    h1, h2, h3 {
        text-align: center;
        color: #333; /* Darker text color for headings */
    }

    img {
        width: 100%; /* Responsive width for images */
        max-width: 30rem;
        height: auto;
        display: block;
        margin: 0 auto;
    }

/* Basic overlay styles */
    .overlay {
        display: none;  /* Hidden by default */
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .zoomImage {
        cursor: pointer;
        transition: transform 0.3s ease-in-out, opacity 0.3s ease-in-out;
    }

    .zoomImage.active {
        transform: scale(2);  /* Zoom effect */
        opacity: 0.9;         /* Fading effect */
    }
</style>

<!-- JavaScript -->
<script>
    const zoomImages = document.querySelectorAll('.zoomImage');
    const overlay = document.querySelector('.overlay');  // Keep .overlay

    zoomImages.forEach(image => {
        image.addEventListener('click', () => {
            if (image.style.transform === 'scale(1)') {
                image.style.transform = 'scale(2)';
                image.style.opacity = '0.9';
                overlay.style.display = 'block';
            } else {
                image.style.transform = 'scale(1)';
                image.style.opacity = '1';
                overlay.style.display = 'none';
            }
        }); 
    });

    overlay.addEventListener('click', () => {
        zoomImages.forEach(image => {
            image.style.transform = 'scale(1)';
            image.style.opacity = '1';
        });
        overlay.style.display = 'none';
    });
</script>